// Quick warm-up activity
// Create an application that takes in a series of numbers then sorts them.
// Feel encouraged to use Stack or Google to find the "sort" code.
// ===========================================================================================
// var fs = require("fs")

// fs.readFile("written.txt", "utf8", function(err, data) {
//     // If there was an error reading the file, we log it and return immediately
//     if (err) {
//       return console.log(err);
//     }
// });
//     const args= process.argv
//     const readArgs=(argv)=>argv[0];{
//         returnargv[0]
//     }
//     var arr= ['1','3','2']
//     function sortarr(arr)
//     arr.sort
//     console.log(arr);
//     let idx=0 ;
//     let array =[];

// given an array sort through and arrange
    let sortArgs = process.argv
    map (it =>parseFloat(it))
    .slice(2)
    .sort((a,b)=>a-b);
console.log(sortArgs);


    
