import React from "react";

function List(props) {
  return (
    <ul className="list-group">
      Using the map method, render one li element displaying the "name" property of each grocery
      object.
    </ul>
  );
}

export default List;
